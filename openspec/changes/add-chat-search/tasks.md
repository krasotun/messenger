## 1. Запрос Чатов по названию

- [x] 1.1 Написать падающие спеки `src/app/domains/chats/infrastructure/chat.api.spec.ts`: `chats({ title })` шлет `GET /chats?title=<title>`, `chats()` без запроса - `GET /chats` без параметров
- [x] 1.2 Написать падающую спеку `src/app/domains/chats/infrastructure/http-chat-gateway.spec.ts`: `chats({ title })` передает название в `ChatApi` и маппит ответ так же, как без запроса
- [x] 1.3 Ввести `src/app/domains/chats/application/chats-query.type.ts`, необязательный запрос в `chat.gateway.ts`, `chat.api.ts` и `http-chat-gateway.ts`; проверить, что спеки блока зеленые, а `chat-list.service.spec.ts` не изменился и зеленый
- [x] 1.4 Научить `GET /chats` в `mock-backend/src/routes/chats.ts` фильтровать по `title` (подстрока без учета регистра) и проверить запросом к запущенному `mock-backend`, что без `title` приходит полный список
- [x] 1.5 Прогнать `npm run lint` и `npm run test:ci`

## 2. Use case и состояние Поиска чата

- [x] 2.1 Написать падающую спеку `src/app/domains/chats/application/search-chats/search-chats.service.spec.ts`: название уходит в шлюз запросом `{ title }`, найденные **Чаты** возвращаются как есть
- [x] 2.2 Написать падающие спеки `src/app/domains/chats/application/chat-search/chat-search.state.spec.ts` по образцу `user-search.state.spec.ts`: пустой ввод - `NotStarted` без запроса; найденные - `Found` вместе с текстом, по которому они найдены; пустой ответ - `NothingFound`; отказ - `NothingFound`, и следующий ввод снова отправляет запрос; ответ на предыдущий ввод не подменяет результат последнего; запрос уходит после задержки
- [x] 2.3 Реализовать `search-chats.service.ts`, `chat-search-status.type.ts` и `chat-search.state.ts`; проверить, что спеки блока зеленые
- [x] 2.4 Прогнать `npm run lint` и `npm run test:ci`

## 3. Результат поиска без текста и ручной режим Popover

- [x] 3.1 Убрать из результата `Found` текст, по которому найдены **Чаты**, в `src/app/domains/chats/application/chat-search/chat-search.state.ts` и поправить `chat-search.state.spec.ts`; проверить, что спеки зеленые
- [x] 3.2 Написать падающие спеки ручного режима в `src/app/shared/ui/popover/popover.spec.ts` на сценарии требования «Ручное открытие» из `shared-ui-popover`: щелчок по хосту ничего не меняет, владелец открывает и закрывает, повторная команда ничего не меняет, Escape и щелчок вне закрывают, Escape при фокусе в поле-хосте закрывает и оставляет фокус; проверить, что существующие спеки покрывают требование «Открытие по щелчку», и дописать недостающие сценарии
- [x] 3.3 Добавить вход режима в `src/app/shared/ui/popover/popover.ts` (`opening: PopoverOpening`, по умолчанию `OnHostClick`); проверить, что спеки блока зеленые, а `selected-chat-header.spec.ts` не изменился и зеленый
- [x] 3.4 Прогнать `npm run lint` и `npm run test:ci`

## 4. Компонент Поиска чата

- [ ] 4.1 Добавить в канвас экрана чатов артборды «поиск: найдены чаты» и «поиск: ничего не найдено» по порядку правки из `design.md` и показать их на согласование до верстки
- [ ] 4.2 Написать падающие спеки `src/app/domains/chats/presentation/chat-search/chat-search.spec.ts`: у поля доступное имя `Search chats`; при пустом поле списка нет; найденные **Чаты** показаны **Аватаром** и названием и ведут ссылкой на адрес **Чата**; пустой результат показывает `No chats found`; очистка поля закрывает список
- [ ] 4.3 Написать падающие спеки на открытие и закрытие в том же файле: результат открывает `Popover`, очистка поля закрывает; щелчок в поле не закрывает; Escape закрывает и оставляет текст, новый ввод открывает снова; щелчок вне поля и списка закрывает; щелчок по найденному **Чату** очищает поле и закрывает список
- [ ] 4.4 Реализовать `chat-search.ts`, `chat-search.html`, `chat-search.scss` (раскладка миксинами `layout`, выпадающий список - `Popover` в ручном режиме на поле, ширина содержимого 288px) с текстами из макета; проверить, что спеки блока зеленые
- [ ] 4.5 Прогнать `npm run lint` и `npm run test:ci`

## 5. Встраивание в список и сквозной сценарий

- [ ] 5.1 Написать падающие спеки в `src/app/domains/chats/presentation/chat-list/chat-list.spec.ts`: поле **Поиска чата** есть при загруженном непустом списке и отсутствует в состояниях «No chats yet» и ошибки загрузки; основной список показывает все **Чаты**, пока поиск показывает найденные
- [ ] 5.2 Встроить `ChatSearch` в `chat-list.html` и `chat-list.ts` между шапкой и списком; проверить, что спеки блока зеленые
- [ ] 5.3 Написать e2e `e2e/chats/search-chat.spec.ts`: у **Текущего пользователя** несколько **Чатов**, он вводит часть названия, щелкает по найденному **Чату**, адрес указывает на этот **Чат**, его шапка показана, поле пустое
- [ ] 5.4 Добавить скриншотный e2e с тегом `@visual` на открытый выпадающий список с найденными **Чатами** в `e2e/chats/chats.screenshot.spec.ts`, снять снимок в arm64-контейнере и сверить его с артбордом «поиск: найдены чаты»; убедиться, что существующий снимок `chats-screen.png` не разошелся
- [ ] 5.5 Прогнать `npm run lint`, `npm run test:ci`, `npm run e2e` и `npm run e2e:visual`

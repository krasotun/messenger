## 1. Подготовка артефакта сборки

- [x] 1.1 Добавить `public/.nojekyll`, чтобы Pages не обрабатывал сборку через Jekyll
- [x] 1.2 Проверить локально: `npm run build -- --base-href /messenger/` завершается успешно, а в `dist/messenger/browser/index.html` присутствует `<base href="/messenger/">`
- [x] 1.3 Добавить в сборку шаг создания `404.html` как копии `index.html` и убедиться, что файл попадает в публикуемый каталог

## 2. Workflow публикации

- [x] 2.1 Заменить в `.github/workflows/deploy.yml` шаги установки Ansible, настройки SSH, проверки цели и запуска playbook на связку `actions/configure-pages`, `actions/upload-pages-artifact`, `actions/deploy-pages`
- [x] 2.2 Добавить `permissions` (`contents: read`, `pages: write`, `id-token: write`), окружение `github-pages` и `concurrency` с группой `pages`
- [x] 2.3 Сохранить порядок проверок перед публикацией: `lint` -> `test:ci` -> `e2e` -> `build`
- [x] 2.4 Убедиться, что сборка в workflow выполняется с нужным `base-href`, а локальная разработка и e2e продолжают работать от корня

## 3. Включение Pages

- [x] 3.1 В настройках репозитория переключить Pages source на GitHub Actions
- [x] 3.2 Запустить deploy workflow вручную (`workflow_dispatch`) и дождаться успешного завершения

## 4. Проверка поведения из спецификации

- [x] 4.1 `https://krasotun.github.io/messenger/` отдает приложение по HTTPS, статика грузится без 404
- [x] 4.2 Прямое открытие и перезагрузка `https://krasotun.github.io/messenger/sign-in` показывают страницу входа
- [x] 4.3 Неизвестный маршрут обрабатывается роутером приложения, а не страницей ошибки хостинга
- [x] 4.4 Живой вход в аккаунт на продакшен-адресе проходит, сессия восстанавливается, защищенный маршрут открывается
- [x] 4.5 Проверить, что при падении любой проверки качества публикация не происходит (например, на отдельной ветке или намеренно проваленным прогоном)

## 5. Документация и архивирование прежнего процесса

- [x] 5.1 Обновить `README.md`: новый продакшен-адрес и новый процесс деплоя вместо мертвой ссылки на VDS, Ansible-команд и секретов `VDS_*`
- [x] 5.2 Пометить `docs/deployment/*` как архивные: явная отметка, что описанный способ деплоя больше не используется
- [x] 5.3 Добавить `ansible/README.md` с пометкой, что каталог сохранен как история и не участвует в деплое
- [x] 5.4 Зафиксировать в документации, что VDS выведен из эксплуатации, секреты `VDS_*` удалены и отката на прежний хостинг нет

## 6. Quality gates

- [x] 6.1 `npm run lint`
- [x] 6.2 `npm run test:ci`
- [x] 6.3 `npm run e2e`
- [x] 6.4 `npx openspec validate deploy-to-github-pages --strict`

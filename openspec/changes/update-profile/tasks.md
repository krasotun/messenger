## 1. Зависимость

- [x] 1.1 Убедиться, что change `shared-ui-modal` реализован и модалка доступна как примитив

## 2. Инфраструктура user API

- [x] 2.1 Написать падающий spec на маппер запроса: редактируемые поля переводятся в snake_case тело запроса
- [x] 2.2 Написать падающий spec на gateway: `PUT /user/profile` вызывается с `withCredentials`
- [x] 2.3 Написать падающий spec: ответ `CurrentUserDTO` мапится в `CurrentUser`
- [x] 2.4 Написать падающий spec: ошибка приводится к `ApplicationError` с текстом из `reason`
- [x] 2.5 Реализовать DTO, мапперы, контракт gateway и его HTTP-реализацию

## 3. Use case обновления профиля

- [x] 3.1 Spec: сервис отдает начальные значения формы из текущего пользователя
- [x] 3.2 Spec: при успехе текущая сессия обновляется из ответа бэкенда
- [x] 3.3 Spec: при успехе вызывающая сторона получает обновленного текущего пользователя
- [x] 3.4 Spec: при успехе навигация не вызывается
- [x] 3.5 Spec: при ошибке текущая сессия не меняется, а сообщение об ошибке доступно наружу
- [x] 3.6 Реализовать use case на `createAuthFlowState`

## 4. Форма редактирования

- [x] 4.1 Component spec: форма предзаполнена значениями текущего пользователя
- [x] 4.2 Component spec: невалидная форма не отправляет запрос и показывает ошибки полей
- [x] 4.3 Component spec: во время отправки форма заблокирована
- [x] 4.4 Component spec: при ошибке введенные значения остаются в форме
- [x] 4.5 Реализовать форму, переиспользуя правила валидации регистрации

## 5. Точка входа и закрытие

- [x] 5.1 Component spec: успешное сохранение приводит к закрытию модалки
- [x] 5.2 Component spec: закрытие без сохранения не меняет текущую сессию
- [x] 5.3 Реализовать открытие формы в модалке из выбранной точки входа
- [x] 5.4 Проверить, что application-слой не импортирует `ModalRef`, overlay и Angular-формы
- [x] 5.5 Component spec: открытие формы закрывает меню текущего пользователя

## 6. Сквозной сценарий

- [x] 6.1 E2E: пользователь меняет данные профиля и видит их в интерфейсе без перезагрузки
- [x] 6.2 Добавить поддержку `PUT /user/profile` в `mock-auth-backend`, если ее нет

## 7. Quality gates

- [x] 7.1 `npm run lint`
- [x] 7.2 `npm run test:ci`
- [ ] 7.3 `npm run e2e`
- [x] 7.4 `npx openspec validate update-profile --strict`

## 1. Зависимость

- [x] 1.1 Убедиться, что change `shared-ui-modal` реализован и модалка доступна как примитив

## 2. Инфраструктура user API

- [x] 2.1 Написать падающий spec на маппер запроса: редактируемые поля переводятся в snake_case тело запроса
- [x] 2.2 Написать падающий spec на gateway: `PUT /user/profile` вызывается с `withCredentials`
- [x] 2.3 Написать падающий spec: ответ `CurrentUserDTO` мапится в `CurrentUser`
- [x] 2.4 Написать падающий spec: ошибка приводится к `ApplicationError` с текстом из `reason`
- [x] 2.5 Реализовать DTO, мапперы, контракт gateway и его HTTP-реализацию

## 3. Use case обновления профиля

- [ ] 3.1 Spec: сервис отдает начальные значения формы из текущего пользователя
- [ ] 3.2 Spec: при успехе текущая сессия обновляется из ответа бэкенда
- [ ] 3.3 Spec: при успехе вызывающая сторона получает обновленного текущего пользователя
- [ ] 3.4 Spec: при успехе навигация не вызывается
- [ ] 3.5 Spec: при ошибке текущая сессия не меняется, а сообщение об ошибке доступно наружу
- [ ] 3.6 Реализовать use case на `createAuthFlowState`

## 4. Форма редактирования

- [ ] 4.1 Component spec: форма предзаполнена значениями текущего пользователя
- [ ] 4.2 Component spec: невалидная форма не отправляет запрос и показывает ошибки полей
- [ ] 4.3 Component spec: во время отправки форма заблокирована
- [ ] 4.4 Component spec: при ошибке введенные значения остаются в форме
- [ ] 4.5 Реализовать форму, переиспользуя правила валидации регистрации

## 5. Точка входа и закрытие

- [ ] 5.1 Component spec: успешное сохранение приводит к закрытию модалки
- [ ] 5.2 Component spec: закрытие без сохранения не меняет текущую сессию
- [ ] 5.3 Реализовать открытие формы в модалке из выбранной точки входа
- [ ] 5.4 Проверить, что application-слой не импортирует `ModalRef`, overlay и Angular-формы

## 6. Сквозной сценарий

- [ ] 6.1 E2E: пользователь меняет данные профиля и видит их в интерфейсе без перезагрузки
- [ ] 6.2 Добавить поддержку `PUT /user/profile` в `mock-auth-backend`, если ее нет

## 7. Quality gates

- [ ] 7.1 `npm run lint`
- [ ] 7.2 `npm run test:ci`
- [ ] 7.3 `npm run e2e`
- [ ] 7.4 `npx openspec validate update-profile --strict`

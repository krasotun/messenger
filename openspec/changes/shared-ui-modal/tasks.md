## 1. Контракт примитива

Файлы примитива: `src/app/shared/ui/modal/modal-service.ts`,
`modal-shell/modal-shell.ts`, `modal-shell/modal-shell.html`,
`modal-shell/modal-shell.scss`, `modal-ref.ts`, `modal-size.ts` и
спеки рядом с ними (`*.spec.ts`).

- [x] 1.1 Написать падающий spec: `open()` с компонентом-содержимым отображает его содержимое в модалке
- [x] 1.2 Реализовать `ModalService.open()` (`modal-service.ts`) поверх CDK Overlay вместо текущей заглушки и `ModalShell` (`modal-shell/`)
- [x] 1.3 Написать падающий spec на `ModalRef`, получаемый содержимым через DI
- [x] 1.4 Реализовать `ModalRef` (`modal-ref.ts`) с единственным методом `close()`
- [x] 1.5 Spec: значения inputs, переданные в `open()`, доходят до содержимого; реализовать проброс через `ComponentRef.setInput`
- [x] 1.6 Ввести тип `ModalSize` (`modal-size.ts`)

## 2. Поведение окна

- [x] 2.1 Spec: при открытой модалке фон затенен, а прокрутка приложения заблокирована
- [x] 2.2 Spec: после закрытия прокрутка восстанавливается
- [x] 2.3 Spec: модалка остается по центру при изменении размера окна
- [x] 2.4 Реализовать центрирование, backdrop и блокировку скролла

## 3. Закрытие

- [x] 3.1 Spec на каждый источник закрытия: кнопка, `Escape`, клик по backdrop, сигнал из содержимого - все дают одно событие закрытия
- [x] 3.2 Spec: клик внутри модалки не закрывает ее
- [x] 3.3 Spec: после закрытия overlay уничтожается
- [x] 3.4 Реализовать обработку закрытия и освобождение overlay

## 4. Фокус

- [ ] 4.1 Spec: после открытия фокус находится внутри модалки
- [ ] 4.2 Spec: `Tab` с последнего элемента и `Shift+Tab` с первого оставляют фокус внутри модалки
- [ ] 4.3 Spec: после закрытия фокус возвращается на элемент, который был в фокусе до открытия
- [ ] 4.4 Реализовать focus trap в шаблоне `modal-shell.html` через `cdkTrapFocus` с `cdkTrapFocusAutoCapture`

## 5. Размеры

- [ ] 5.1 Spec: заданный пресет задает ширину; без пресета применяется `medium`
- [ ] 5.2 Spec: высота следует за содержимым и меняется вместе с ним
- [ ] 5.3 Реализовать пресеты `small` / `medium` / `large` в `modal-shell.scss` и `modal-size.ts`

## 6. Единственность

- [ ] 6.1 Spec: повторный `open()` при активной модалке не создает вторую модалку и не влияет на открытую
- [ ] 6.2 Реализовать защиту от повторного открытия

## 7. Границы слоя

- [ ] 7.1 Проверить, что `shared/ui/modal` не импортирует домены, `core` и `pages`
- [ ] 7.2 Убедиться, что правила `no-restricted-imports` покрывают этот случай

## 8. Quality gates

- [ ] 8.1 `npm run lint`
- [ ] 8.2 `npm run test:ci`
- [ ] 8.3 `npx openspec validate shared-ui-modal --strict`

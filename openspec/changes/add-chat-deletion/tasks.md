# Задачи: удаление чата

Секция - единица ревью: одна ветка блока и один PR в ветку issue #109.

Порядок: сначала общий примитив подтверждения, затем транспорт и роль, затем
экран, затем мок-бэкенд и сквозные сценарии. Мок нужен только для e2e -
component- и application-тесты работают на подмененном HTTP.

## 1. Примитив подтверждения в shared/ui

- [ ] 1.1 Написать падающие тесты
      `src/app/shared/ui/confirm/confirm-modal-content.spec.ts` на сценарии
      «Действие подтверждено», «Отказ кнопкой», «Тексты задает вызывающий»;
      проверка - тесты падают до реализации и проходят после.
- [ ] 1.2 Создать `src/app/shared/ui/confirm/confirm-options.ts` и
      `confirm-modal-content.ts` с разметкой вопроса (заголовок, текст,
      кнопки отказа и подтверждения, место под ошибку); проверка - тесты 1.1
      зеленые.
- [ ] 1.3 Написать падающий тест
      `src/app/shared/ui/confirm/confirm-service.spec.ts` на исход
      подтверждения и отказа, включая сценарий «Окно закрыто, а не отвечено»
      (`Escape`, фон, кнопка закрытия); проверка - тест падает до реализации.
- [ ] 1.4 Реализовать `ConfirmService.confirm(options): Observable<boolean>`
      поверх `ModalService`; проверка - тесты 1.3 зеленые, публичный экспорт
      добавлен и `ModalService.open()` не менялся.
- [ ] 1.5 Дописать падающие тесты на сценарии «Действие выполняется»,
      «Действие завершилось успехом», «Действие завершилось ошибкой», «Повтор
      после ошибки», «Отказ после ошибки»; проверка - тесты падают.
- [ ] 1.6 Реализовать поддержку `options.action`: блокировка кнопок и запрет
      закрытия на время действия, показ **Ошибки приложения**, закрытие при
      успехе; проверка - тесты 1.5 зеленые.
- [ ] 1.7 `npm run lint`, `npm run test:ci`.

## 2. Создатель чата и DELETE /chats

- [ ] 2.1 Дописать падающий тест в
      `src/app/domains/chats/infrastructure/http-chat-gateway.spec.ts` на
      чтение `created_by` из ответа `GET /chats`; проверка - тест падает.
- [ ] 2.2 Добавить `createdBy` в `application/chat.ts`, `created_by` в
      `infrastructure/chat.dto.ts` и `chat.mapper.ts`; проверка - тест 2.1
      зеленый, существующие спеки маппера не сломаны.
- [ ] 2.3 Дописать падающий тест в `http-chat-gateway.spec.ts` на
      `DELETE /chats` с телом `{ chatId }`; проверка - тест падает.
- [ ] 2.4 Добавить `deleteChat` в `application/chat.gateway.ts`,
      `infrastructure/chat.api.ts` и `http-chat-gateway.ts`; проверка - тест
      2.3 зеленый.
- [ ] 2.5 Написать падающий тест
      `application/delete-chat/delete-chat.service.spec.ts` на успех и на
      проброс **Ошибки приложения**; проверка - тест падает.
- [ ] 2.6 Реализовать `DeleteChatService` и `DeleteChatInput`; проверка -
      тест 2.5 зеленый.
- [ ] 2.7 `npm run lint`, `npm run test:ci`.

## 3. Удаление чата в шапке выбранного чата

- [ ] 3.1 Дописать падающие тесты
      `presentation/selected-chat-header/selected-chat-header.spec.ts` на
      сценарии «Чат создан текущим пользователем» и «Чат создан другим
      пользователем»; проверка - тесты падают.
- [ ] 3.2 Показать действие в `selected-chat-header.html` и вычислить признак
      «я - **Создатель чата**» сравнением `currentUser()?.id` из
      `CurrentSessionService` с `createdBy` **Чата**; проверка - тесты 3.1
      зеленые.
- [ ] 3.3 Дописать падающие тесты на сценарии «Подтверждение отклонено»,
      «Чат удален» (перезапрос списка и уход с адреса) и «Удаление
      отклонено»; проверка - тесты падают.
- [ ] 3.4 Связать действие с `ConfirmService` и `DeleteChatService`, после
      успеха вызвать `ChatListService.loadChats()` и увести навигацию на `/`;
      проверка - тесты 3.3 зеленые.
- [ ] 3.5 `npm run lint`, `npm run test:ci`.

## 4. Мок-бэкенд

- [ ] 4.1 Хранить **Создателя чата** в `mock-backend/src/store.ts` и отдавать
      `created_by` в `GET /chats`; проверка - ответ для созданного **Чата**
      содержит id создателя.
- [ ] 4.2 Реализовать `DELETE /chats`: 403 не создателю, удаление **Чата** и
      его состава создателю; проверка - удаленный **Чат** исчезает из
      `GET /chats`.
- [ ] 4.3 Дописать в `docs/api/README.md` заметку о том, что `created_by` есть
      в примерах `swagger.json`, но не в `properties` схемы `ChatsResponse`;
      проверка - заметка на месте и названа дата проверки боевого API.
- [ ] 4.4 `npm run lint`.

## 5. Сквозные сценарии

- [ ] 5.1 Написать `e2e/chats/delete-chat.spec.ts`: создатель удаляет **Чат**,
      подтверждает и больше не видит его в списке; проверка - `npm run e2e`
      зеленый.
- [ ] 5.2 Написать скриншотный тест `e2e/chats/delete-chat.screenshot.spec.ts`
      с тегом `@visual` на открытое окно подтверждения; проверка -
      `npm run e2e:visual` зеленый, снимок пересмотрен глазами.
- [ ] 5.3 Quality gates: `npm run lint`, `npm run test:ci`, `npm run e2e`,
      `npm run e2e:visual`.
